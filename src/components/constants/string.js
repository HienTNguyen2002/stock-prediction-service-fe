const INVALID_TICKER = 'Ticker is invalid'
const PRIOR_INVALID = 'Prior must be between 0 and 1'
const START_DATE_INVALID = "Date that prediction starts must be before the max date of stock index"
const TRAINING_YEARS_INVALID = "Training years must be within range"
const MA_INVALID = "Moving Average must be greater than 0"

export {
    INVALID_TICKER,
    PRIOR_INVALID,
    START_DATE_INVALID,
    TRAINING_YEARS_INVALID,
    MA_INVALID
}

export default {
    INVALID_TICKER,
    PRIOR_INVALID,
    START_DATE_INVALID,
    TRAINING_YEARS_INVALID,
    MA_INVALID
}